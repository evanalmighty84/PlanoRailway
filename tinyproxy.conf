Port 8888
Listen 0.0.0.0
Timeout 600
DisableViaHeader Yes
LogLevel Info

# Allow all (safe because this service is only on Railway's private network)
Allow 0.0.0.0/0

# Ensure HTTPS CONNECT works
ConnectPort 443
ConnectPort 563
